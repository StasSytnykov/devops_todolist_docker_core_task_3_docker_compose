# Guidline

To manipulate with this docker compose you need to use this command 
```commandline
docker compose up -d 
```
this command will run your compose file

```commandline
docker compose stop
```
using this command you can stop and delete running containers